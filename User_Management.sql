1. Create a user
```bash
CREATE USER 'akash'@'localhost' IDENTIFIED BY 'password';
```
> **Note:** 
> 1. ```'akash'@'localhost'```: The user (```akash```) can access MySQL only from the ```localhost```
> 2. 
  4. ```localhost``` is not included in the ```%``` wildcard.
